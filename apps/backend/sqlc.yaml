version: "2"
sql:
  - engine: "sqlite"
    queries: "db/queries.sql"
    schema: "db/schema.sql"
    gen:
      go:
        package: "db"
        out: "domain/session/sqlite/db"
        emit_json_tags: false
        emit_interface: false
