# Voldemorbot Docs

Hugo documentation site for the Voldemorbot v2 project, deployed to [voldemorbot.teamvoldemor.com](https://voldemorbot.teamvoldemor.com/).

## Prerequisites

- [Hugo Extended](https://gohugo.io/installation/) 0.156.0+
- [Go](https://golang.org/) 1.18+ (for `scripts/copy_docs.go`)
- [Task](https://taskfile.dev/) 3.x (for the Taskfile workflow)
- Git (for submodule checkout)

## Quick start

```sh
task serve
```

This initialises the theme submodule, syncs Markdown from `../docs` into `content/v1/`, and starts the Hugo dev server at <http://localhost:1313/>.

## Taskfile reference

All common operations are automated via [Taskfile.yml](Taskfile.yml). Run `task` to list available commands.

### `task theme:init` (alias: `theme`)
Init the `hugo-book` theme submodule.

### `task content:sync` (alias: `copy-docs`)
Copy Markdown files from `../docs` into `content/v1/`, auto-injecting YAML frontmatter when missing. Powered by `scripts/copy_docs.go`.

### `task content:clean` (alias: `clean`)
Remove all generated files under `content/v1/`.

### `task site:serve` (alias: `serve`)
Start Hugo dev server with live reload. Depends on `theme:init` + `content:sync`.

### `task site:build` (alias: `build`)
Incremental Hugo build to `public/`.

### `task site:build-prod` (alias: `build:prod`)
Production build with minification (`hugo --minify`).

### `task site:clean` (alias: `clean:public`)
Remove the `public/` build output directory.

### `task code:lint` (alias: `lint`)
Run Biome linter.

### `task code:fmt` (alias: `fmt`)
Format files with Biome (writes in place).

### `task code:check` (alias: `check`)
Run Biome lint + format check (CI-safe, no writes).

### `task deploy:cloudflare` (alias: `cloudflare`)
Build for Cloudflare Pages. Output goes to `public/`; connect the repo in the Cloudflare dashboard.

## Deployment

Deployment is handled automatically by GitHub Actions on every push to `master`
that touches `hugo-docs/`. See `.github/workflows/hugo-docs.yml`.

Required repository secrets:

| Secret | Description |
|---|---|
| `CLOUDFLARE_API_TOKEN` | Cloudflare API token with Pages edit permission |
| `CLOUDFLARE_ACCOUNT_ID` | Cloudflare account ID |

## Project structure

```
hugo-docs/
├── assets/
│   ├── custom/             # 20+ SCSS partials (brand, header, tabs, code, etc.)
│   ├── *_custom.scss       # Entry point importing all partials
│   ├── code-copy.js
│   ├── lightbox.js
│   ├── scroll-utils.js
│   ├── tabs.js
│   ├── theme-switcher.js
│   └── toast.js
├── content/
│   └── v1/                 # Synced Markdown (gitignored, generated by copy_docs.go)
├── layouts/
│   ├── _default/_markup/   # Blockquote render hook
│   ├── partials/
│   │   ├── book-sidebar-footer.html
│   │   ├── components/     # back-to-top
│   │   ├── docs/           # brand, header-custom, page-header, sidebar-custom,
│   │   │                   # theme-switcher, toc
│   │   ├── head-includes.html
│   │   └── version-switcher.html
│   ├── shortcodes/         # 13 custom shortcodes (note, warning, tip, danger,
│   │                       # info, alert, success, question, important, example,
│   │                       # tab, tabs, etc.)
│   ├── 404.html
│   └── baseof.html         # Custom base template overriding hugo-book
├── scripts/
│   └── copy_docs.go        # Content sync helper with front-matter injection
├── static/
│   └── images/             # Static images (project-diagram.png, etc.)
├── themes/
│   └── hugo-book/          # Git submodule (pinned)
├── config.toml             # Site configuration
└── Taskfile.yml            # Task automation (see reference above)
```
