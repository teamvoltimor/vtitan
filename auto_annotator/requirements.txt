# SAM2 Interactive Annotator dependencies
# Install with: pip install -r requirements.txt

# Core ML / inference
torch>=2.3.0
torchvision

# SAM 2.1 (Meta's native package — preferred over Ultralytics for embedding caching)
# Downloads sam2.1_s.pt automatically on first run, or place it manually next to app.py
sam2

# UI
gradio>=4.44.0   # earlier 4.x has a regression where Image.select() returns [None, None]

# Image processing
opencv-python
numpy
Pillow
