# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc

from telemetry.v1 import commands_pb2 as telemetry_dot_v1_dot_commands__pb2


class RobotCommandServiceStub(object):
    """RobotCommandService pushes backend-queued commands down to a robot.

    Protocol: the robot always dials out and opens StreamCommands once at
    startup (and again on reconnect); the backend never initiates a
    connection to the robot. The stream is best-effort — if it drops, the
    robot keeps racing/navigating standalone and simply has no live command
    channel until reconnect. last_command_id lets a reconnecting robot
    resume without replaying commands it already applied.

    The robot acks every command it receives via AckCommand on the same
    logical connection (a separate unary call), so the backend can surface
    CommandResult (see domain/robot.CommandResult) back through the REST API.
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.StreamCommands = channel.unary_stream(
                '/vtitan.telemetry.v1.RobotCommandService/StreamCommands',
                request_serializer=telemetry_dot_v1_dot_commands__pb2.StreamCommandsRequest.SerializeToString,
                response_deserializer=telemetry_dot_v1_dot_commands__pb2.RobotCommand.FromString,
                _registered_method=True)
        self.AckCommand = channel.unary_unary(
                '/vtitan.telemetry.v1.RobotCommandService/AckCommand',
                request_serializer=telemetry_dot_v1_dot_commands__pb2.AckCommandRequest.SerializeToString,
                response_deserializer=telemetry_dot_v1_dot_commands__pb2.AckCommandResponse.FromString,
                _registered_method=True)


class RobotCommandServiceServicer(object):
    """RobotCommandService pushes backend-queued commands down to a robot.

    Protocol: the robot always dials out and opens StreamCommands once at
    startup (and again on reconnect); the backend never initiates a
    connection to the robot. The stream is best-effort — if it drops, the
    robot keeps racing/navigating standalone and simply has no live command
    channel until reconnect. last_command_id lets a reconnecting robot
    resume without replaying commands it already applied.

    The robot acks every command it receives via AckCommand on the same
    logical connection (a separate unary call), so the backend can surface
    CommandResult (see domain/robot.CommandResult) back through the REST API.
    """

    def StreamCommands(self, request, context):
        """StreamCommands is opened once by the robot; the backend pushes each
        queued RobotCommand as it is dispatched. Bounded per-call deadline is
        intentionally NOT set client-side by the robot — the server enforces a
        generous keepalive/idle timeout instead so the stream survives for the
        life of the robot process.
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def AckCommand(self, request, context):
        """AckCommand reports the outcome of executing a previously streamed
        command. Fire-and-forget from the robot's perspective: a failed ack
        delivery must never block command execution.
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_RobotCommandServiceServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'StreamCommands': grpc.unary_stream_rpc_method_handler(
                    servicer.StreamCommands,
                    request_deserializer=telemetry_dot_v1_dot_commands__pb2.StreamCommandsRequest.FromString,
                    response_serializer=telemetry_dot_v1_dot_commands__pb2.RobotCommand.SerializeToString,
            ),
            'AckCommand': grpc.unary_unary_rpc_method_handler(
                    servicer.AckCommand,
                    request_deserializer=telemetry_dot_v1_dot_commands__pb2.AckCommandRequest.FromString,
                    response_serializer=telemetry_dot_v1_dot_commands__pb2.AckCommandResponse.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'vtitan.telemetry.v1.RobotCommandService', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('vtitan.telemetry.v1.RobotCommandService', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class RobotCommandService(object):
    """RobotCommandService pushes backend-queued commands down to a robot.

    Protocol: the robot always dials out and opens StreamCommands once at
    startup (and again on reconnect); the backend never initiates a
    connection to the robot. The stream is best-effort — if it drops, the
    robot keeps racing/navigating standalone and simply has no live command
    channel until reconnect. last_command_id lets a reconnecting robot
    resume without replaying commands it already applied.

    The robot acks every command it receives via AckCommand on the same
    logical connection (a separate unary call), so the backend can surface
    CommandResult (see domain/robot.CommandResult) back through the REST API.
    """

    @staticmethod
    def StreamCommands(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_stream(
            request,
            target,
            '/vtitan.telemetry.v1.RobotCommandService/StreamCommands',
            telemetry_dot_v1_dot_commands__pb2.StreamCommandsRequest.SerializeToString,
            telemetry_dot_v1_dot_commands__pb2.RobotCommand.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def AckCommand(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/vtitan.telemetry.v1.RobotCommandService/AckCommand',
            telemetry_dot_v1_dot_commands__pb2.AckCommandRequest.SerializeToString,
            telemetry_dot_v1_dot_commands__pb2.AckCommandResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
