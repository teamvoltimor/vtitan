# generated by datamodel-codegen:
#   filename:  navigation/blind_nav/localization.schema.json

from __future__ import annotations

from pydantic import ConfigDict, Field
from shared.config.codegen_base import StrictModel


class NavigationBlindNavLocalization(StrictModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    search_radius_m: float = Field(
        ..., description='half-width of the initial pose search window'
    )
    passes: int = Field(..., description='coarse-to-fine grid-search passes')
    grid_points: int = Field(..., description='candidates per axis per search pass')
    residual_clip_m: float = Field(
        ..., description='per-ray residual clipping distance (outlier rejection)'
    )
    max_speed_mps: float = Field(
        ...,
        description='Implausible-jump speed bound (m/s): a pose update implying more than this is discarded and the prior pose kept; ~1.5x the closed-loop ceiling.',
    )
    jump_confirm_tolerance_m: float = Field(
        ...,
        description='how close two consecutive rejected candidates must be to confirm a real correction',
    )
    relocalize_confirm_dist_m: float = Field(
        ...,
        description="Distance (m) beyond which a global relocalization winner is HELD instead of taken on the scan that found it, and accepted only when a later global search lands within jump_confirm_tolerance_m of it. relocalize_min_width_spread_m tests max-min of the four believed widths, which is not the same question as whether the model is SYMMETRIC: run_20260915_160804 believed 0.600/0.600/1.000/1.000, a spread of 0.400 that passes easily while the model is exactly mirror-symmetric about both axes, so every pose has twins that explain the scan as well as it does. (0.63/0.955/0.958 are that round's TRUE spans measured off /scan; an earlier version of this text cited them as the belief, which was wrong.) The estimate teleported 2.06 m in 1.26 s, the corridor label flipped east to west, the cost fell 0.0316 to 0.0100 so relocalize_accept_ratio was satisfied, and the planner then replanned 26 waypoints backwards and swept 213 degrees of yaw in 4.8 s inside an 8 x 26 cm box: the U-turn on the round that scored 6. WHAT IT ACTUALLY IS, measured over every scan of both available bags rather than intended: NOT a reconvergence test. At one scan's lag the genuine rescue reconverges within 5 cm on 95.5% of 555 pairs and the twin on 80-95% of its own, so both would confirm. It works because the second search is at least relocalize_after_scans later, and by then the winner has moved WITH the robot (median 0.309 m against the robot's 0.335 m) against an uncompensated 5 cm tolerance: at its real cadence it is a STATIONARITY test, and the 160804 twin episode lasted 3 scans while the car moved 0.3 m. Do NOT add motion compensation, which would restore the refuted test and accept the twin. COST: on the one genuine rescue (run_20260907_205830) the first confirmation lands at 10.5 s, not 1.5 s; still far better than the 48 s divergence it recovered. The real lever is upstream and is not fixed here: the event only reproduces when a post-stall yaw is paired with a pre-stall scan, and 1 degree of yaw flips the winner between twins 2.0 m apart at costs 6% apart. Set to 0.0 to restore the unconditional behaviour.",
    )
    relocalize_cost_threshold: float = Field(
        ...,
        description='Mean clipped squared residual (m^2, real returns only) at which a scan counts as not explaining the pose, arming global relocalization.',
    )
    relocalize_after_scans: int = Field(
        ..., description='consecutive bad scans before re-solving globally (~1.5 s)'
    )
    relocalize_grid_step_m: float = Field(
        ..., description='free-space candidate spacing for the global search'
    )
    relocalize_accept_ratio: float = Field(
        ...,
        description='Fraction of the local cost the global fit must beat before it replaces the local pose; otherwise the wall model is presumed wrong.',
    )
    relocalize_min_width_spread_m: float = Field(
        ...,
        description='Minimum spread (max minus min) among the four believed corridor widths before a global relocalization may run at all. Below it the believed free space is 4-fold rotationally symmetric, so four poses explain any scan equally well and the search is not merely expensive, it is ill posed: MEASURED on run_20260915_140358 it teleported the estimate from (1.5, 0.1) to (1.75, 2.93) -- the 180 degree rotational copy about the track centre -- and flipped yaw by 179 degrees, and two winners 1.96 m apart scored within 0.4% of each other. The Obstacles Challenge fixes all four corridors at 1000 mm, so the spread there is exactly 0 and the search never runs; Open varies them (600/1000), and on run_20260907_205830, the round whose estimate was lost for 48 s and which this search recovered, the spread is 0.40 m for 91% of ticks, from t=7.5s. The 9% below are the opening ticks before any width is estimated, where the model really is symmetric. Set to 0.0 to restore the unconditional behaviour.',
    )
