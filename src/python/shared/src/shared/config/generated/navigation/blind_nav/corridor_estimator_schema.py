# generated by datamodel-codegen:
#   filename:  navigation/blind_nav/corridor_estimator.schema.json

from __future__ import annotations

from pydantic import ConfigDict, Field
from shared.config.codegen_base import StrictModel


class NavigationBlindNavCorridorEstimator(StrictModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    min_samples: int = Field(
        ...,
        description='Width readings a corridor must accumulate, attributed by heading, before its width estimate is trusted.',
    )
    plausible_width_margin_m: float = Field(
        ...,
        description='Margin (m) outside the legal [NARROW, WIDE] band within which a reading is a plausibility failure (a corner), not accepted.',
    )
    max_start_samples: int = Field(
        ...,
        description='Length of the rolling window of stationary width readings kept before the start button, so only readings after placement count.',
    )
    decision_boundary_m: float = Field(
        ...,
        description='Width (m) at which a raw measurement snaps to WIDE rather than NARROW; defaults to the midpoint of the two legal widths.',
    )
