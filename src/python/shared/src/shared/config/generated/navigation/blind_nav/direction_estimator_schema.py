# generated by datamodel-codegen:
#   filename:  navigation/blind_nav/direction_estimator.schema.json

from __future__ import annotations

from pydantic import ConfigDict, Field
from shared.config.codegen_base import StrictModel


class NavigationBlindNavDirectionEstimator(StrictModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    alignment_tolerance_rad: float = Field(
        ...,
        description='Maximum heading error (rad) against the nearest track axis for a side-ray reading to be trusted; shared with width measurement.',
    )
    corner_clearance_m: float = Field(
        ...,
        description='Forward clearance (m) below which the corridor counts as ended, opening the window to read which side is open; kept above turn_clearance_m.',
    )
    max_in_track_range_m: float = Field(
        ...,
        description='Side rays longer than this (m) are rejected rather than voted on; a LIDAR dropout is substituted with max range and would read as open.',
    )
    min_asymmetry_m: float = Field(
        ...,
        description='Minimum left/right range difference (m) for a sweep to count as evidence rather than noise.',
    )
    plausible_span_threshold_m: float = Field(
        ...,
        description='Maximum left + right range sum (m) that still represents one corridor; a larger sum means one ray ran off into an adjacent corridor.',
    )
    min_votes: int = Field(
        ...,
        description='Agreeing observations required before the estimator settles on a direction; voting, not a single scan.',
    )
    gate_log_period_ticks: int = Field(
        ...,
        description='Log one direction-gate verdict every this many unresolved blind-creep ticks; diagnostic only.',
    )
