# generated by datamodel-codegen:
#   filename:  navigation/sensors/start_measurement.schema.json

from __future__ import annotations

from pydantic import ConfigDict, Field
from shared.config.codegen_base import StrictModel


class NavigationSensorsStartMeasurement(StrictModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    ray_half_width_deg: float = Field(
        ...,
        description="wedge half-width for each cardinal ray's median (converted to radians at point of use)",
    )
    closing_tolerance_m: float = Field(
        ...,
        description='how far forward+back may fall short of the mat before a reading is rejected',
    )
    retry_window_s: float = Field(
        ...,
        description='how long after the direction commit a refused measurement keeps being retried',
    )
    retry_align_tolerance_deg: float = Field(
        ...,
        description="how far off the start corridor's bearing a retried measurement is still believed",
    )
