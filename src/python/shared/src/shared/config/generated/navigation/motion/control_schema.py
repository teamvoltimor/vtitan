# generated by datamodel-codegen:
#   filename:  navigation/motion/control.schema.json

from __future__ import annotations

from pydantic import ConfigDict, Field
from shared.config.codegen_base import StrictModel


class NavigationMotionControl(StrictModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    control_hz: float = Field(
        ...,
        description='Frequency (Hz) the control loop runs at, on the robot and in simulation.  The single source for the loop period. This used to be stated twice -- 0.05 s in the waypoint controller and 20 Hz in the simulated gateway -- which agreed only by maintenance. The controller rate-limits steering with dt, so the two drifting apart would not raise anything: it would tune the robot against a cadence the simulator never ran at.',
    )
