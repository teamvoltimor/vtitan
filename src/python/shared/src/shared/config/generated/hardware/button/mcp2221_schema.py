# generated by datamodel-codegen:
#   filename:  hardware/button/mcp2221.schema.json

from __future__ import annotations

from pydantic import ConfigDict, Field
from shared.config.codegen_base import StrictModel

from ... import _models as field_models


class Button(StrictModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    pull_up: bool = Field(
        ...,
        description='Use the GPIO internal pull-up; the button is wired GND to pin, so a press pulls the pin low.',
    )
    debounce_ms: int = Field(
        ...,
        description='Debounce window in milliseconds that filters electrical contact noise.',
    )
    long_press_threshold_sec: float = Field(
        ...,
        description='Hold time in seconds after which a press is treated as long (E-STOP while racing, reset once finished).',
    )
    shutdown_press_threshold_sec: float = Field(
        ...,
        description='Kept in step with gpio.toml, which is the driver actually wired into button_node -- see its comments for why these two values are what they are.',
    )


class HardwareButtonMcp2221(StrictModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    mcp2221: field_models.Mcp2221
    button: Button
