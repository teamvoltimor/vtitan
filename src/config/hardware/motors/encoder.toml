#:schema ../../../model/hardware/motors/encoder.schema.json
# Quadrature encoder + closed-loop PID. Per-motor calibration lives in the profile overlays.
# Rationale: adr:0076-drivetrain-and-steering-hardware

pin_a = 16
pin_b = 20
max_duty = 0.5
