// Code generated by github.com/atombender/go-jsonschema, DO NOT EDIT.

package blind_nav

type NavigationBlindNavCorridorEstimator struct {
	// Width at which a raw measurement snaps to WIDE rather than NARROW. Defaults to
	// the midpoint of the two legal widths (0.8 m against a 0.03 m LIDAR sigma,
	// better than 4-sigma either way), but is tunable independently of them --
	// shifting it off-centre trades a false NARROW for a false WIDE without moving
	// either corridor width.
	DecisionBoundaryM float64 `json:"decision_boundary_m" yaml:"decision_boundary_m" mapstructure:"decision_boundary_m"`

	// Length of the rolling window of stationary width readings kept before the start
	// button is pressed. Enough to outvote the narrow prior comfortably (min_samples
	// above), and at the 20 Hz control rate it spans the last second before the
	// button is pressed. A window rather than a total, because the robot is often
	// powered on well away from the track and only what it sees once placed should
	// count.
	MaxStartSamples int `json:"max_start_samples" yaml:"max_start_samples" mapstructure:"max_start_samples"`

	// Width readings a corridor must accumulate before its estimate is trusted.
	// Readings are attributed to a corridor by heading, so a handful taken while the
	// chassis is still swinging through a corner can land in the wrong one. Requiring
	// a dozen costs about half a second at the 20 Hz control loop and makes a single
	// mis-filed sweep unable to move the answer.
	MinSamples int `json:"min_samples" yaml:"min_samples" mapstructure:"min_samples"`

	// How far outside the legal [NARROW, WIDE] band a measured width may still fall
	// and be treated as a plausibility failure (a corner, not a noisy corridor)
	// rather than accepted.
	PlausibleWidthMarginM float64 `json:"plausible_width_margin_m" yaml:"plausible_width_margin_m" mapstructure:"plausible_width_margin_m"`
}
