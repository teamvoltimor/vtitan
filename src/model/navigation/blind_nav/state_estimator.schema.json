{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vtitan.local/schemas/navigation/blind_nav/state_estimator.schema.json",
  "title": "NavigationBlindNavStateEstimator",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "yaw_correction_gain": {
      "type": "number",
      "description": "Fraction of the observed yaw discrepancy folded into the dead-reckoning estimate per update; deliberately small so one bad correction cannot drag heading.",
      "x-journal": "adr:0054-absolute-heading-from-walls"
    }
  },
  "required": [
    "yaw_correction_gain"
  ]
}
