{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vtitan.local/schemas/hardware/motors/encoder.schema.json",
  "title": "HardwareMotorsEncoder",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "pin_a": {
      "type": "integer",
      "description": "BCM GPIO pin for the encoder's A channel."
    },
    "pin_b": {
      "type": "integer",
      "description": "BCM GPIO pin for the encoder's B channel."
    },
    "max_duty": {
      "type": "number",
      "description": "Safety ceiling on the closed-loop PID's output magnitude, as a fraction of full duty (0-1)."
    }
  },
  "required": [
    "pin_a",
    "pin_b",
    "max_duty"
  ]
}
