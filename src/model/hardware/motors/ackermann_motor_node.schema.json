{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vtitan.local/schemas/hardware/motors/ackermann_motor_node.schema.json",
  "title": "HardwareMotorsAckermannMotorNode",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "publisher_rate_hz": {
      "type": "number",
      "description": "Rate in Hz at which the motor node publishes actuator state (steering, drive speed, joint states)."
    },
    "diagnostics_rate_hz": {
      "type": "number",
      "description": "Rate in Hz at which the motor node publishes /motor/status diagnostics (much slower than publishing)."
    }
  },
  "required": [
    "publisher_rate_hz",
    "diagnostics_rate_hz"
  ]
}
