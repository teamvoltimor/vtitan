{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vtitan.local/schemas/hardware/display/oled_node.schema.json",
  "title": "HardwareDisplayOledNode",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "ui_refresh_rate_hz": {
      "type": "number",
      "description": "OLED redraw cadence in Hz; the telemetry bridge's ui_summary_rate_hz is matched to it.",
      "x-journal": "adr:0083-oled-backend-and-button-thresholds"
    },
    "display_backend": {
      "type": "string",
      "description": "SSD1306 I2C backend: blinka (Adafruit CircuitPython) or raw_i2c (direct /dev/i2c-N ioctl).",
      "x-journal": "adr:0083-oled-backend-and-button-thresholds"
    }
  },
  "required": [
    "ui_refresh_rate_hz",
    "display_backend"
  ]
}
