{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vtitan.local/schemas/hardware/state_machine/state_machine_node.schema.json",
  "title": "HardwareStateMachineStateMachineNode",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "challenge_mode_samples_required": {
      "type": "integer",
      "description": "Consecutive agreeing BOOT_CHECK-tick samples required before trusting the challenge-mode jumper reading."
    },
    "challenge_mode_timeout_sec": {
      "type": "number",
      "description": "Seconds to wait for the Pi Zero's jumper reading before defaulting to Open Challenge."
    }
  },
  "required": [
    "challenge_mode_samples_required",
    "challenge_mode_timeout_sec"
  ]
}
