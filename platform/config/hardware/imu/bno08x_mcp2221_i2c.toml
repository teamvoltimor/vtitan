# BNO08x IMU via MCP2221A I2C bridge -- src/hardware/imu/bno08x/mcp2221/i2c.py
# Not the actively deployed IMU backend (see bno08x_mcp2221_uart_rvc.toml),
# but this was the code's own default -- kept unchanged, just now visible.

imu_i2c_address = "0x4A"
