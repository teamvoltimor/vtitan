// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: telemetry/v1/commands.proto

package telemetryv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	RobotCommandService_StreamCommands_FullMethodName = "/vtitan.telemetry.v1.RobotCommandService/StreamCommands"
	RobotCommandService_AckCommand_FullMethodName     = "/vtitan.telemetry.v1.RobotCommandService/AckCommand"
)

// RobotCommandServiceClient is the client API for RobotCommandService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// RobotCommandService pushes backend-queued commands down to a robot.
//
// Protocol: the robot always dials out and opens StreamCommands once at
// startup (and again on reconnect); the backend never initiates a
// connection to the robot. The stream is best-effort — if it drops, the
// robot keeps racing/navigating standalone and simply has no live command
// channel until reconnect. last_command_id lets a reconnecting robot
// resume without replaying commands it already applied.
//
// The robot acks every command it receives via AckCommand on the same
// logical connection (a separate unary call), so the backend can surface
// CommandResult (see domain/robot.CommandResult) back through the REST API.
type RobotCommandServiceClient interface {
	// StreamCommands is opened once by the robot; the backend pushes each
	// queued RobotCommand as it is dispatched. Bounded per-call deadline is
	// intentionally NOT set client-side by the robot — the server enforces a
	// generous keepalive/idle timeout instead so the stream survives for the
	// life of the robot process.
	StreamCommands(ctx context.Context, in *StreamCommandsRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[RobotCommand], error)
	// AckCommand reports the outcome of executing a previously streamed
	// command. Fire-and-forget from the robot's perspective: a failed ack
	// delivery must never block command execution.
	AckCommand(ctx context.Context, in *AckCommandRequest, opts ...grpc.CallOption) (*AckCommandResponse, error)
}

type robotCommandServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewRobotCommandServiceClient(cc grpc.ClientConnInterface) RobotCommandServiceClient {
	return &robotCommandServiceClient{cc}
}

func (c *robotCommandServiceClient) StreamCommands(ctx context.Context, in *StreamCommandsRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[RobotCommand], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &RobotCommandService_ServiceDesc.Streams[0], RobotCommandService_StreamCommands_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[StreamCommandsRequest, RobotCommand]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type RobotCommandService_StreamCommandsClient = grpc.ServerStreamingClient[RobotCommand]

func (c *robotCommandServiceClient) AckCommand(ctx context.Context, in *AckCommandRequest, opts ...grpc.CallOption) (*AckCommandResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(AckCommandResponse)
	err := c.cc.Invoke(ctx, RobotCommandService_AckCommand_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// RobotCommandServiceServer is the server API for RobotCommandService service.
// All implementations should embed UnimplementedRobotCommandServiceServer
// for forward compatibility.
//
// RobotCommandService pushes backend-queued commands down to a robot.
//
// Protocol: the robot always dials out and opens StreamCommands once at
// startup (and again on reconnect); the backend never initiates a
// connection to the robot. The stream is best-effort — if it drops, the
// robot keeps racing/navigating standalone and simply has no live command
// channel until reconnect. last_command_id lets a reconnecting robot
// resume without replaying commands it already applied.
//
// The robot acks every command it receives via AckCommand on the same
// logical connection (a separate unary call), so the backend can surface
// CommandResult (see domain/robot.CommandResult) back through the REST API.
type RobotCommandServiceServer interface {
	// StreamCommands is opened once by the robot; the backend pushes each
	// queued RobotCommand as it is dispatched. Bounded per-call deadline is
	// intentionally NOT set client-side by the robot — the server enforces a
	// generous keepalive/idle timeout instead so the stream survives for the
	// life of the robot process.
	StreamCommands(*StreamCommandsRequest, grpc.ServerStreamingServer[RobotCommand]) error
	// AckCommand reports the outcome of executing a previously streamed
	// command. Fire-and-forget from the robot's perspective: a failed ack
	// delivery must never block command execution.
	AckCommand(context.Context, *AckCommandRequest) (*AckCommandResponse, error)
}

// UnimplementedRobotCommandServiceServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedRobotCommandServiceServer struct{}

func (UnimplementedRobotCommandServiceServer) StreamCommands(*StreamCommandsRequest, grpc.ServerStreamingServer[RobotCommand]) error {
	return status.Errorf(codes.Unimplemented, "method StreamCommands not implemented")
}
func (UnimplementedRobotCommandServiceServer) AckCommand(context.Context, *AckCommandRequest) (*AckCommandResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method AckCommand not implemented")
}
func (UnimplementedRobotCommandServiceServer) testEmbeddedByValue() {}

// UnsafeRobotCommandServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to RobotCommandServiceServer will
// result in compilation errors.
type UnsafeRobotCommandServiceServer interface {
	mustEmbedUnimplementedRobotCommandServiceServer()
}

func RegisterRobotCommandServiceServer(s grpc.ServiceRegistrar, srv RobotCommandServiceServer) {
	// If the following call pancis, it indicates UnimplementedRobotCommandServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&RobotCommandService_ServiceDesc, srv)
}

func _RobotCommandService_StreamCommands_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(StreamCommandsRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(RobotCommandServiceServer).StreamCommands(m, &grpc.GenericServerStream[StreamCommandsRequest, RobotCommand]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type RobotCommandService_StreamCommandsServer = grpc.ServerStreamingServer[RobotCommand]

func _RobotCommandService_AckCommand_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(AckCommandRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(RobotCommandServiceServer).AckCommand(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: RobotCommandService_AckCommand_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(RobotCommandServiceServer).AckCommand(ctx, req.(*AckCommandRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// RobotCommandService_ServiceDesc is the grpc.ServiceDesc for RobotCommandService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var RobotCommandService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "vtitan.telemetry.v1.RobotCommandService",
	HandlerType: (*RobotCommandServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "AckCommand",
			Handler:    _RobotCommandService_AckCommand_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "StreamCommands",
			Handler:       _RobotCommandService_StreamCommands_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "telemetry/v1/commands.proto",
}
