# Toolchain + build output. The tracked source is circuit.tsx; everything
# here is regenerated by `tsci build` / `tsci export`.
node_modules/
dist/
.tscircuit/
*.svg
*.png
*.netlist.txt
package-lock.json
